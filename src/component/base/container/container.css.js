// =============================================================================
// Import config.
// =============================================================================
import {LAYOUT_WIDTH} from '/config/layout.config';

export const BASE = {
  maxWidth: LAYOUT_WIDTH,
  marginLeft: 'auto',
  marginRight: 'auto',
};
